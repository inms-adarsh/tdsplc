(function ()
{
    'use strict';

    angular
        .module('app.dashboards.customers',
            [
                // 3rd Party Dependencies
            ]
        )
        .config(config);

    /** @ngInject */
    function config($stateProvider, msApiProvider)
    {
        
    }

})();